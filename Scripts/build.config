# iOS 构建配置文件
# 复制此文件到你的项目根目录，并根据项目修改

# 项目类型: xcodeproj 或 xcworkspace
PROJECT_TYPE="xcodeproj"

# 项目文件名（包含相对路径）
PROJECT_NAME="../HabitKit.xcodeproj"

# Scheme 名称
SCHEME="HabitKit"

# Bundle ID（用于启动应用）
BUNDLE_ID="com.edge.habitkit"

# 构建配置: Debug 或 Release
CONFIGURATION="Debug"

# 目标平台: iOS, macOS, watchOS, tvOS
PLATFORM="iOS"

# 构建目录（相对于项目根目录）
BUILD_DIR="../build"

# 是否在安装后自动启动应用
AUTO_LAUNCH=true

# 额外的 xcodebuild 参数（可选）
# EXTRA_BUILD_ARGS="-xcconfig custom.xcconfig"
EXTRA_BUILD_ARGS=""
